import { LightningElement } from 'lwc';

export default class Looping extends LightningElement {
   //looping works with arrays only.key is must
   
   carList=['Ford','Audi','WV','Hyundai','Mercedes','Ranger'];
}